# Spec: <feature name>

Status: Draft
Issue: MickMarch/medialab#<N>

Allowed statuses: Draft, Approved, Shipped, Superseded. No implementation code
before Approved. No version numbers anywhere in a spec.

## Problem

What is wrong or missing, with the observation that motivated it.

## Goal and non-goals

One paragraph each.

## Design

The behaviour, inputs, outputs, and state changes. Name the services touched
and what each one owns. Prefer a table for endpoints and a state diagram for
lifecycle changes.

## Decisions

Numbered. Each decision states the alternative rejected and why.

## Open questions

Numbered. Resolve before Approved, or move to Decisions with the answer.

## Test plan

The failing tests to write first, by repo.

## Rollout

Order of repo PRs, any migration, any manual host step.
